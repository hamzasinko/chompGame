#ifndef MATRICE_TEST_H
#define MATRICE_TEST_H

// Function to test the 'remplissage' function which initializes the matrix
// Returns 1 if the test passes, 0 otherwise
int test_remplissage();

// Function to test the 'affichage' function which handles the display of the matrix
// Returns 1 if the test passes, 0 otherwise
int test_affichage();

#endif // MATRICE_TEST_H
